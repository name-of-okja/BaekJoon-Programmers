#include <iostream>

using namespace std;

int main()
{
    double a, b, result;
    cin >> a >> b;
    result = a / b;
    cout.precision(10);
    cout << result << endl;
    return 0;
}
